
<%
var hideTitle = hideTitle || false;
var hideMenu = hideMenu || false;
%>

<!doctype html>
<html>
	<head>
		<title>LXC Admin</title>
		<%- style('bootstrap') %>
	</head>
	<body>
    <div class="container mt-5">
      <% if (!hideTitle) { %>
        <h1>LXC Admin</h1>
      <% } %>
      <div class="row mt-3">
        <% if (hideMenu) { %>
          <div class="col-12">
        <% } else { %>
          <div class="col-sm-3">
		<%- await include('./nav.ejs') %>
          </div>
          <div class="col-sm-9">
  	<% } %>
